.venv/
build/
dist/
__pycache__/
*.pyc
.env
proyectos/
guiones/
error.log
.DS_Store

# llave privada de licencias — NUNCA subir
LLAVE_PRIVADA_NO_COMPARTIR.hex
licencia.txt

# binarios de ffmpeg (los descarga el CI; localmente se conservan)
empaquetado/ffmpeg/
empaquetado/ffmpeg-win/
licencias_emitidas.json

# datos de usuario del nicho Relax (presets guardados)
relax_presets.json

# sonidos reales descargados (banco Relax, se bajan de archive.org)
sonidos/

# cachés del nicho Relax (descargas del usuario)
visuales/
musica/
